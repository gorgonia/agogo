package online

/*
func TestNewLayer(t *testing.T) {
	visibles := []Desc{
		{2, 2, 3, 2, 2, true},
		{2, 2, 3, 2, 2, true},
		{2, 2, 3, 2, 2, true},
	}
	l := NewLayer(2, 2, 3, visibles)
	t.Logf("%d", len(l.FeedBwdWeights))
	for i, v := range l.FeedBwdWeights {
		t.Logf("\t%d: %v", i, len(v))
		for j, v2 := range v {
			t.Logf("\t\t%d: %v", j, len(v2))
		}
	}
}
*/
